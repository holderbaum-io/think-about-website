<header>
  <section>
    <div class="centered">
      <%= content 'elevator'  %>
      <% if feature? :preview %>
      <a button href="<%= t('tickets.shop_link.link') %>"><%= t('nav.tickets') %></a>
      <% else %>
      <p><strong><%= t('tickets.tba') %></strong></p>
      <% end %>
    </div>
  </section>
</header>
